namespace UserService.Api;

/// <summary>
/// Names this assembly for a test host.
/// </summary>
/// <remarks>
/// A marker rather than making the generated <c>Program</c> public: both
/// services would then declare a type of the same name in the global
/// namespace, and a test project referencing both would not compile.
/// </remarks>
public sealed class ServiceEntryPoint;
